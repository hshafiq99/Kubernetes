apiVersion: v1
kind: Service
metadata:
  name: ingress-svc
  labels:
    app: ingress-svc
spec:
  type: NodePort
  ports:
    - port: 80
      name: http
      targetPort: 443
    - port: 443
      name: https
      targetPort: 443
  selector:
    app: web